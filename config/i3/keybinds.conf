# i3 window-management binds — mirrors ~/dots/keybindings.md (merge into ~/.config/i3/config)
# Assumes: set $mod Mod4   (Super = the GUI thumb key, matching Cmd/Win)

# focus (vim dirs)
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# maximize: M = almost (floating, centered-ish), N = full
bindsym $mod+Shift+m floating toggle
bindsym $mod+Shift+n fullscreen toggle

# terminal + launcher
bindsym $mod+Return exec --no-startup-id ghostty
bindsym $mod+space exec --no-startup-id rofi -show drun

# layout
bindsym $mod+v split toggle
bindsym $mod+Shift+space floating toggle
bindsym $mod+Shift+q kill

# resize (quick nudges; or use a proper resize mode)
bindsym $mod+u resize shrink width 4 px or 4 ppt
bindsym $mod+p resize grow width 4 px or 4 ppt
bindsym $mod+i resize grow height 4 px or 4 ppt
bindsym $mod+o resize shrink height 4 px or 4 ppt

# workspaces
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6

# reload / restart
bindsym $mod+Shift+r reload
